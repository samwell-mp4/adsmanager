[variables]
NIXPACKS_NODE_VERSION = '20'

[phases.install]
cmds = ['npm install --include=dev']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start --workspace=@browser-manager/api'
